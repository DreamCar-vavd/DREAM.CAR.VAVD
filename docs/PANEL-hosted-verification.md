# Протокол реальної hosted-перевірки панелі

Виконується **після** погодженого підключення GitHub App
(`docs/PANEL-hosting-and-approvals.md` §3), на **Preview**-деплої гілки
`codex/admin-panel-spike`. Production і `main` не залучені.

**Не активувати** workflow, що переносить контент у `main`
(`content-guard.yml`), до завершення цього протоколу — перша перевірка має
лишитися повністю ізольованою (`PANEL_CONTENT_BRANCH` веде в окрему гілку, не
в `main`).

Позначки результату: ✅ пройдено · ❌ ні · ⚠️ з відхиленням.

| # | Крок | Очікуваний результат | Як перевірити | Стан (оновлено 2026-09-15) |
|---|---|---|---|---|
| 1 | **Вхід власника** | `/keystatic` → Sign in with GitHub → Authorize → список колекцій; `/panel` → дашборд | відкрити обидві сторінки, побачити 6 розділів у `/panel` | ✅ |
| 2 | **Вхід помічника** | те саме у другому браузері / акаунті (роль Write) | помічник відкриває `/keystatic`, бачить ті самі колекції | ✅ (`VOLODYMYR-LUCKY`, Write) |
| 3 | **Відмова сторонньому** | репозиторій **публічний** — GitHub віддає читання будь-якому автентифікованому токену незалежно від Collaborator-статусу ([GitHub docs: About repositories](https://docs.github.com/en/repositories/creating-and-managing-repositories/about-repositories)), тож «repo not found» на самому читанні репо НЕ очікується для жодного облікового запису; критерій — заборона **запису** (Keystatic save), заборона **заявок** (`/panel/leads`) і фолбек захищеного **чернеткового перегляду** (draft-preview), той самий `assertWriteAccess()`-гейт, що й для відкликаного Collaborator | сторонній акаунт → `/keystatic` Sign in → Authorize → спроба редагувати / `/panel/leads` / draft-preview | ⚠️ **критерій виправлено 15.09** — розділено на дві незалежні частини (нижче) |
| 3a | ↳ **чинні права для облікового запису без Collaborator-доступу** | той самий `StorageForbiddenError`, що й для відкликаного Collaborator (GitHub API не розрізняє «ніколи не мав» і «мав, відкликали» — обидва дають `permissions.push:false` на публічному читанні) | — | ✅ **покрито рядками 18–21** — код перевіряє лише `permissions.push`, не історію акаунта, тож жива перевірка `VOLODYMYR-LUCKY` доводить той самий код-шлях і для облікового запису, який ніколи не мав доступу |
| 3b | ↳ **перший OAuth-вхід (Sign in → Authorize) облікового запису, який ще НІКОЛИ не проходив авторизацію застосунку** | Authorize завершується без 404 (до фіксу GitHub App був Private → 404 для будь-кого, крім власника; після фіксу — Public) | третій акаунт, який ще не встановлював/не авторизовував GitHub App, → `/keystatic` → Sign in | ❌ **не перевірено після фіксу** — потрібен саме НЕвстановлений раніше акаунт (не `VOLODYMYR-LUCKY`, який уже проходив Authorize під час цієї сесії); не вимагаю створення третього акаунта без потреби — залишаю як точково відкритий пункт |
| 4 | **Створення синтетичного матеріалу** | нова картка (напр. послуга `zzz-test`) зберігається | Keystatic → Послуги → Create → заповнити 3 мови → Save | ✅ |
| 5 | **Збереження через реальний GitHub** | Save робить коміт у гілку `PANEL_CONTENT_BRANCH` (не `main`) | GitHub → гілки → бачити новий коміт з файлом `services/zzz-test.json` | ✅ |
| 6 | **Мовні підтвердження** | `/panel` → картка показує 3 мови «Потребує перевірки» → «Позначити перевіреним» × 3 → «Перевірено»; повторне збереження тексту знову «Потребує перевірки» | клацнути по кожній мові, потім змінити текст у Keystatic і оновити `/panel` | ✅ |
| 7 | **Актуальна чернетка з новими фото** | `/panel` → «Переглянути чернетку на сайті» показує `zzz-test` та нове фото, яких немає в опублікованому | відкрити draft-preview, знайти картку | ✅ |
| 8 | **Тестова публікація за конкретним SHA** | «Опублікувати зміни» на `zzz-test` → `published.json` у гілці контенту оновлюється; банер стану показує збірку Preview за цим SHA | `/panel` → Опублікувати → дочекатися «✅ на тестовому сайті»; звірити SHA у банері з гілкою | ✅ |
| 9 | **Правильний deployment** | банер каже саме «на тестовому сайті», НЕ «в ефірі»; `environment` = Preview | подивитися текст банера + `deployStatus` | ✅ |
| 10 | **Конфлікт двох редакторів** | обидва відкрили `/panel`; A публікує `zzz-test`; B (стара версія) публікує ту саму картку → **повідомлення про конфлікт + кнопка «Оновити»**, без тихого перезапису | двоє одночасно, B тисне «Опублікувати» після A | ✅ **жива перевірка 13.09**, синтетичні записи, ОДИН акаунт (`DreamCar-vavd`) у двох вкладках — задокументовано прямо. Пройдено **три** варіанти: (а) стара опублікована версія, той самий контент → безпечний no-op «вже опубліковано в цій версії», нуль зайвих комітів; (б) стара review-версія (до підтверджень мов) → клієнт взагалі блокує кнопку «Опублікувати» локально, запит на сервер не йде; (в) **14.09** — B завантажив сторінку, коли картка вже була готова до публікації (кнопка активна), потім A в іншій вкладці змінив текст, повторно підтвердив мови й опублікував нову версію — B (не оновлюючи сторінку) тисне свою досі активну кнопку → сервер відповідає `HTTP 409`, `ConflictError` («Дані «контент» змінилися...»), картка лишається версією A. Це саме той випадок, де клієнтське блокування кнопки (варіант б) не спрацьовує — рятує лише серверна перевірка `workingVersion`/`reviewVersion` у `publishItem()`. Усі три підтверджено на GitHub: рівно один коміт `published.json` за раз, нуль зайвих. Тестові записи (`zzz-test`, потім `zzz-conflict-test`) прибрано обидва рази, net-zero |
| 11 | **Відкликання доступу** | у помічника відкликати App (Authorized GitHub Apps → Revoke) або прибрати з Collaborators | виконати, потім крок 12 | ✅ (прибрано з Collaborators) |
| 12 | **Немає доступу до чернеток після виходу** | (а) Collaborator прибрано → `/panel`/`/panel/leads`/draft-preview відмовляють з `StorageForbiddenError` («недостатньо прав доступу…» / «Немає прав доступу для перегляду чернетки»); (б) GitHub-App-авторизацію відкликано (`Authorized GitHub Apps → Revoke`) → `NotConnectedError`/`StorageAuthError` («Сесію завершено або відкликано») | прибрати з Collaborators АБО відкликати App, потім оновити `/panel`, `/panel/leads`, draft-preview | ✅/⚠️ **узгоджено 15.09.** Варіант (а) — **підтверджено живо, реальним, чинним, відкликаним акаунтом** (`VOLODYMYR-LUCKY`, Collaborator прибрано): усі три поверхні + Keystatic-save відмовляють, рядки 18–21, фото `IMG_1415/1422/1423/1424.JPG`. Це і закриває суть цього рядка. Варіант (б) (App-авторизація відкликана саме ПІД ЧАС уже відкритої сесії — «був доступ → відкликали → стара сесія/кукі лишились») **код-рівнем покрито** (`assertWriteAccess()` викликається на кожному рендері, незалежно від draft-mode-кукі чи попереднього стану сесії — перевірка сама по собі не має пам'яті про те, коли кукі виставлено) і покрито тестами `draftAccess.test.ts` (без сесії/з фейковим токеном → відмова); але **живого циклу саме в такому порядку** (мати доступ → увімкнути перегляд чернетки → відкликати App-авторизацію мідсесії → перевірити, що стара кукі більше не працює) **цієї сесії не проводили** — не називаю це відтвореним. Функціонально код-шлях ідентичний тому, що вже підтверджено живо для (а) (та сама перевірка на кожному рендері), тому ризик низький, але це висновок із дизайну коду, а не окрема жива перевірка |
| 13 | **Прибирання** | видалити синтетичну картку в Keystatic + прибрати з `published.json` (Unpublish у `/panel`) + прибрати рядок review-state.json («Завершити видалення» у `/panel`), звірити `git diff` контенту з базовим SHA — не лише кількість матеріалів чи `content:guard` | Keystatic delete + `/panel` «Прибрати з сайту» + «Завершити видалення»; `git diff <база>..<поточний> -- src/content/` | ✅ **(14.09, після виправлення)** — `zzz-test` (13.09) і `zzz-conflict-test` (14.09) обидва прибрано повністю: `git diff c8dbf56..f76e2f3 -- src/content/` показує РІВНО одну зміну (`published.json.publishedAt` — очікуваний побічний ефект живих publish-дій, не тестове сміття), `review-state.json` побайтово ідентичний базі. Попереднє формулювання цього рядка спиралося лише на `content:guard`-кількість (3 авто/8 галерея/5 послуг) — цього виявилось недостатньо: `content:guard` не бачить review-state.json, тому й пропустив залишковий `service:zzz-conflict-test` до diff-перевірки 14.09. Надалі — саме `git diff` проти конкретного SHA, не кількість |
| 16 | **Завершення "Delete entry" в Keystatic (review-state сирота)** | Keystatic "Delete entry" видаляє картку, але ніколи не чіпає `review-state.json` — кнопка `/panel` «Завершити видалення» прибирає осиротілий рядок | видалити картку в Keystatic → `/panel` показує банер «Незавершені видалення» → натиснути «Завершити видалення» → банер зникає | ✅ **14.09** — `service:zzz-conflict-test` лишався в `review-state.json` від попередньої сесії (Keystatic delete 13.09 не чіпав review-state, а «Завершити видалення» тоді не натиснули). Кнопка спрацювала з першого разу без дефектів: commit `f76e2f3`, банер зник, `review-state.json` після — побайтово ідентичний базі `c8dbf56` (рядок 13 вище) |
| 18 | **Keystatic save реальним безправним акаунтом (частина блокера А)** | `VOLODYMYR-LUCKY` (дійсний GitHub-акаунт, залогінений, БЕЗ Collaborator-доступу) відкриває картку на `codex/admin-panel-spike` в Keystatic і тисне «Зберегти» → відмова запису, коміту немає | власник відкрив окремий профіль Chrome із живою сесією `VOLODYMYR-LUCKY`, за покроковою інструкцією асистента: гілка `codex/admin-panel-spike` → «Послуги» → `car-selection` → змінити «Порядок показу» → «Зберегти» → фотографія результату | ✅ **14.09, задача 12:51** — показано нативний діалог Keystatic **«Fork Repo: You don't have permission to write to this repo so to save your changes, you need to fork the repo»**. Це підтверджує: акаунт дійсний (не фейковий/відсутній токен — інакше було б перенаправлення на Sign in, а не ця форма), і GitHub API справді відмовив у записі. Коміту не створено — звірено: `origin/codex/admin-panel-spike` до і після спроби — той самий SHA `08e9809`. Фото збережено: `/Users/apple/Downloads/IMG_1422.JPG` (мітка часу на екрані — 14:47:11 BST). **Обмеження доказу:** підтвердження — фотографії фізичного екрана (окремий Chrome-профіль недоступний інструментам асистента — точна причина в блоці «Блокер А» нижче), не пряма автоматизована перевірка; природа відмови (Keystatic-рівень при GitHub write API, а не власний `assertWriteAccess()` панелі) підтверджує ту саму відсутність прав, але це ІНШИЙ код-шлях, ніж `/panel`/`/panel/leads` — див. рядок 19 |
| 19 | **`/panel/leads` реальним безправним акаунтом (частина блокера А)** | `VOLODYMYR-LUCKY` відкриває `/panel/leads` на Preview → відмова власного коду панелі (`assertWriteAccess()`), не дашборд заявок | власник відкрив `/panel/leads` у тому самому профілі, сфотографував результат | ✅ **14.09, задача 15:02** — власник уже мав це фото до звернення (не перезнімалося на повторну вимогу): `/Users/apple/Downloads/IMG_1415.JPG`, мітка часу 14:27:45 BST, URL у адресному рядку — `.../panel/leads`, повідомлення: **«GitHub відхилив запит: недостатньо прав доступу для перегляду заявок. Оновіть сторінку; якщо повторюється — перевірте з власником репозиторію права доступу застосунку.»** — це точний текст `StorageForbiddenError` (403, `store/adapter.ts`), тобто саме код-шлях `assertWriteAccess()`, який захищає `/panel/leads` — ІНШИЙ від Keystatic-шляху в рядку 18. **SHA на момент фото:** 14:27:45 BST = 13:27:45 UTC; найближчий успішний Preview-деплой до цього часу — `08e9809` (`created_at` 13:05:44 UTC, ~22 хв запасу), наступний деплой `182f2e8` стартував лише о 13:51:38 UTC (~24 хв після фото) — тобто діючим був саме `08e9809`. **Обмеження:** GitHub Deployments API не дає точної миті «стало активним», лише час створення деплою й статус success у той самий момент — SHA визначено із впевненістю на основі запасу часу, не гарантовано до секунди. Фото попереднього кроку (IMG_1414, 14:25:35 BST) підтверджує вхід і правильну гілку `codex/admin-panel-spike` перед цим |
| 20 | **`/panel` реальним безправним акаунтом (частина блокера А)** | `VOLODYMYR-LUCKY` відкриває `/panel` на Preview → відмова, не дашборд публікації | власник відкрив `.../panel` (без `/leads`) у тому самому профілі за прямою адресою, сфотографував результат | ✅ **14.09, задача 15:32** — `/Users/apple/Downloads/IMG_1423.JPG` (наступне фото після IMG_1422 у тій самій серії), URL у адресному рядку — рівно `.../panel`, той самий текст `StorageForbiddenError`, що й у рядку 19, плюс кнопка «Оновити стан». Часову мітку на цьому фото не видно (обрізана рамка вікна) — SHA прив'язую інакше: `git diff 2c8165f..98fa039 -- 'src/**/*.ts' 'src/**/*.tsx'` (без тестів) — **порожньо**, тобто жоден код, що впливає на `assertWriteAccess()`/`StorageForbiddenError`, не змінювався від коміту з фото рядка 19 (`08e9809`) аж до поточного (`98fa039`) — увесь цей проміжок часу діяла та сама логіка, тож точна секунда деплою тут не критична для висновку. **Разом з рядками 18–19 це закриває всі три незалежні код-шляхи `assertWriteAccess()`-гейту (`/panel`, `/panel/leads`, Keystatic-save) живим безправним акаунтом.** Залишається лише draft-preview (окремий виклик `readSiteContent()`, не покритий жодним із трьох) |
| 21 | **Draft-preview реальним безправним акаунтом — ОСТАННІЙ сценарій блокера А** | `VOLODYMYR-LUCKY` вмикає перегляд чернетки на Preview → фолбек на опублікований сайт з банером про відсутність прав, НЕ робочий неопублікований контент | власник увімкнув перегляд чернетки в тому самому профілі за прямою адресою `/api/panel/preview?enable=1`, сфотографував результат | ✅ **14.09, задача після 15:53** — `/Users/apple/Downloads/IMG_1424.JPG`, 16:29:19 BST, URL — `.../uk` (опублікований сайт, НЕ чернетка), червоний банер: **«Немає прав доступу для перегляду чернетки. Показано опубліковану версію. Вийти з перегляду»**. Звірено дослівно з кодом: `draftAccess.ts:41` → `"Немає прав доступу для перегляду чернетки."` (гілка `StorageForbiddenError`, НЕ `NotConnectedError`/`StorageAuthError`, у яких інший текст — «Сесію завершено або відкликано») + `DraftPreviewBanner.tsx:33` → `{error} Показано опубліковану версію.` — рядок у рядок. Це остаточно підтверджує: **чинний токен без прав** (не відсутній/фейковий), і саме гілка коду `resolveDraftAccess()`, яку раніше покривали лише типізовані автотести. Код не змінювався від попереднього рядка (HEAD того самого коміту `f1b6ecd`, нуль diff у коді). **Разом з рядками 18–20 це закриває блокер А повністю — усі чотири незалежні сценарії живого безправного акаунта підтверджено фотографіями** |
| 17 | **Залежності панелі (next/sharp/js-yaml) оновлено й перевірено на Preview** | Панель не має лишатись на вразливих версіях, поки чекає прийняття PR №26 окремо від основного сайту | `npm ci` з чистого lockfile + tsc/eslint/442 тести/build/content:guard/`npm audit`, потім живо на Preview: `/panel`, `/panel/leads`, draft-preview (увімк./перегляд/вихід), 3 мови, фото+галерея, Keystatic | ✅ **14.09** — `next` 16.3.0→16.3.4, `sharp` 0.35.3→0.35.4 (транзитивний, через `next`), `js-yaml` 4.3.1→4.3.2 (транзитивний, через `@keystatic/core`+`eslint`) — лише `npm update`, без заміни `package.json` для sharp/js-yaml (лишились транзитивними, як і в оригінальних Dependabot PR), `package-lock.json` diff обмежений next+sharp+js-yaml та їхніми platform-сабпакетами (звірено grep'ом). Keystatic/`pg`/`@vercel/blob`/`--experimental-test-module-mocks` — не зачеплені. `npm audit` → 0 vulnerabilities (для цього конкретного скану на цей момент, не гарантія відсутності всіх вразливостей). commit `2c8165f`, CI (Verify+Vercel) — success, deployment `id 6435765725` → `https://dreamcarvavd-4c4rx7kcs-6y7h9wdz4r-7375s-projects.vercel.app` (200 на `/uk`). Живо: `/panel` і `/panel/leads` — без регресій; draft-preview — банер коректний, uk/en/ru рендеряться, зображення (hero/сервіси/галерея/cars-for-sale) — усі 200/304, 0 помилок консолі; Keystatic (`Послуги`) відкривається, 5 чистих карток, без сирітських записів. Синтетичний матеріал для перевірки збереження НЕ створювався — оновлення (patch-версії next/sharp, dev-only js-yaml) не зачіпають шлях запису через GitHub API, і build+lint+живе завантаження редактора вже підтверджують працездатність без додаткового запису |
| 14 | **Мережевий збій під час перевірки чернетки не валить сторінку** | `resolveDraftAccess()` кидав `StorageUnavailableError`/`StorageRateLimitedError` (timeout, rate-limit) НЕ спійманими — виклик у `readSiteContent()` стоїть до її власного `try/catch`, тож сторінка падала цілком замість фолбеку на опублікований контент | код-рівень: `draftAccess.test.ts` (класифікація) + `siteContent.test.ts` (виробничий шлях, `next/headers`/`server-only`/`./store` замоковано) | ✅ **виправлено й перевірено 14.09** — `resolveDraftAccess` тепер класифікує `StorageBackendError && retriable` (той самий патерн, що вже в `leads/page.tsx`) окремо від «немає прав»/«сесію відкликано»; `readSiteContent` додатково обгорнутий власним `try/catch`. commit `fc34d74`. **14.09, друга задача:** старий тест «sequence» лише симулював послідовність усередині самого тесту (не викликав `readSiteContent`) — замінено на `siteContent.test.ts`, що викликає РЕАЛЬНУ `readSiteContent()` із замоканими `server-only`/`next/headers`/`./store`; регресійність підтверджено вручну — тимчасово прибрано зовнішній `try/catch` у `siteContent.ts`, тест на невідомий виняток одразу впав із сирою помилкою (включно з фейковим «паролем» у ній), фікс повернуто, тест знову зелений. commit `df22c31`. **Живої відмови реальним мережевим збоєм GitHub не відтворено** — немає безпечного способу викликати справжній timeout/rate-limit на GitHub API з цього боку без зміни GitHub App/мережі (поза межами задачі); підтвердження — лише код-рівень (усі 442 тести проходять; типізація/білд/lint чисті) |
| 15 | **Синтетична заявка видима в `/panel/leads` (не лише порожній список)** | створити один рядок у тестовій Neon з унікальним ID, оновити `/panel/leads`, побачити саме цей рядок, потім прибрати саме його за ID | Neon SQL Editor (owner-сесія, вже авторизована в браузері — без `.env.migrate`, без секретів у чаті) | ✅ **виправлено й перевірено 14.09** — попередній варіант рядка помилково передбачав лише шлях через `.env.migrate`+адаптер; він відсутній (видалений попередньою сесією), але в браузері вже авторизована консоль Neon (`console.neon.tech`, owner-сесія) з доступом до проєкту `dream-car-leads-test` → SQL Editor. Цей шлях одночасно дає і створення (INSERT), і прибирання (DELETE за id) — на відміну від `leads_app` (лише SELECT/INSERT), тому обіцянку повного циклу прибирання виконано без розширення прав `leads_app`. Хід: `INSERT INTO leads (…) VALUES (…) RETURNING id` → `id = 727a75e2-e6bf-4834-82af-03617a84297e` → `/panel/leads` (owner) показує РІВНО цей рядок з усіма полями (ім'я, телефон, email, послуга, унікальне повідомлення-маркер) → `DELETE FROM leads WHERE id = '727a75e2-…' RETURNING id` (точковий, не `LIKE 'DELETE ME%'`) → `/panel/leads` знову «Заявок поки немає». Побічно виявлено (не дефект, не чіпав): 2 рядки від 10.09.2026 («ZZZ Test», `DELETE ME 21:20 A/B`) уже коректно soft-deleted (`deleted_at` заповнено 11.09) з попередньої сесії — `count(*)` без фільтра бачить їх, `/panel/leads` (яка фільтрує `deleted_at IS NULL`) — ні; поза межами цього завдання |

**Критерій приймання hosted-режиму:** кроки 1–21, і `main` за весь час не
змінився (`ce1977af`, звірено багаторазово перед і після кожної дії цієї
сесії й попередніх, включно з прямими commit/push). **Узгоджено 15.09: 21 із
22 leaf-критеріїв — ✅** (кроки 1–2, 4–21 повністю; крок 3 переформульовано на
дві незалежні частини — 3a ✅, 3b відкритий, деталі в таблиці). **Крок 12
тепер підтверджено живо реальним безправним акаунтом за головним сценарієм**
(Collaborator прибрано → відмова на всіх трьох поверхнях + Keystatic-save);
другий, менш імовірний варіант (App-авторизацію відкликано САМЕ ПІД ЧАС уже
відкритої сесії) лишається код-рівневим висновком, не окремою живою
перевіркою — див. рядок 12 в таблиці, не називаю це відтвореним.

**Блокер А — ЗАКРИТО (14.09, після задачі 15:53):** живий сеанс
`VOLODYMYR-LUCKY` (реальний GitHub-акаунт, без Collaborator) підтвердив
усі чотири незалежні сценарії поточного (не історичного) стану доступу —
**(а) поточний доступ дійсного акаунта без прав, підтверджений
фотографіями:**
- Keystatic save на `codex/admin-panel-spike` → «Fork Repo», без коміту (рядок 18, фото `IMG_1422.JPG`);
- `/panel/leads` → `StorageForbiddenError` (рядок 19, фото `IMG_1415.JPG`, вже надане власником заздалегідь);
- `/panel` → та сама `StorageForbiddenError` (рядок 20, фото `IMG_1423.JPG`);
- draft-preview → фолбек на опублікований сайт з банером «Немає прав доступу для перегляду чернетки» (рядок 21, фото `IMG_1424.JPG`) — текст звірено дослівно з кодом (`draftAccess.ts`/`DraftPreviewBanner.tsx`).

Код-рівень і автотести (StorageAuthError/StorageForbiddenError, окремо
типізовані) покривали ці шляхи й раніше; тепер це підтверджено й живо,
реальним (не фейковим) токеном, за всіма чотирма сценаріями.

**(б) історичний перехід «був доступ → відкликали мідсесії → стара
сесія/кукі лишились»** цим сеансом окремо НЕ відтворювався — `VOLODYMYR-LUCKY`
вже мав Collaborator-доступ прибраний ДО спроб відкрити сторінки, не під час
уже активного сеансу перегляду. Код перевіряє `assertWriteAccess()` на
кожному рендері незалежно від часу видачі кукі/сесії (див. рядок 12), тож
функціонально це той самий код-шлях — але це висновок із будови коду, я не
називаю сам живий цикл «був доступ → відкликали мідсесії» відтвореним, бо
саме такого порядку дій ніхто не виконував.

**Залишок поза блокером А:** лише **крок 3b** — перший OAuth-вхід
(Sign in → Authorize) облікового запису, який ще ніколи не проходив
авторизацію цього GitHub App, після фіксу Private→Public — не перевірявся
жодною сесією. Крок 3a (чинні права стороннього/відкликаного акаунта) вже
покрито тими самими доказами, що й блокер А (той самий код-шлях — GitHub
API не розрізняє «ніколи не мав доступу» від «мав, відкликали», обидва
дають `permissions.push:false`). Не позначаю 3a невиконаним і не вимагаю
третього акаунта для 3b без конкретної причини — сам пункт лишається
названим і точково відкритим.

**Діагноз браузера (виправлено 14.09, задача 15:02; уточнено задачею
15:32):** попереднє формулювання «причина не встановлена» замінили на
«причина відома й не є дефектом коду» — і ЦЕ формулювання теж було
занадто категоричним, слушно вказано контролером. Точний стан: два
однакові результати (`list_connected_browsers` повертає лише 1 запис,
`switch_browser` — «No other browsers available») — це два виклики ТИХ
САМИХ функцій у тому самому середовищі, а не дві незалежні перевірки.
Вони НЕ доводять відсутність проблеми зв'язку з мого боку — систематична
проблема з'єднання виглядала б однаково. Найімовірніше пояснення
лишається те саме (розширення Claude in Chrome ізольоване по профілях
Chrome — має бути окремо встановлене й залогінене в профілі
`VOLODYMYR-LUCKY`), бо це відповідає задокументованій поведінці
`switch_browser` («send a connection request to every Chrome browser
with the extension installed») і не суперечить жодному спостереженню.
Але в мене немає незалежного способу перевірити зв'язок цього інструмента
з розширенням окремо від самих цих двох викликів — тому не стверджую
«доведено, не дефект», а лише «найімовірніша причина, не виключено
інше». Дій із кодом не робив (немає підтвердженого дефекту, який можна
було б виправити), і не буду видавати повторні виклики
`list_connected_browsers`/`switch_browser` за нову перевірку — вони
дадуть той самий результат, доки хтось не встановить розширення в тому
профілі або не з'явиться інший спосіб діагностики.

**Дії для повного закриття блокера А — виконано** (рядки 18–21 вище); дій із
коду не було, лише фото. Дія браузерного інструмента (встановити розширення
в профілі `VOLODYMYR-LUCKY`) лишається відкритою як окреме, менш пріоритетне
покращення — вона потрібна лише для того, щоб надалі такі перевірки міг
виконувати асистент сам, а не для приймання PR №26.

Після успіху — окреме рішення власника про активацію `content-guard.yml` у
`.github/workflows/` + ruleset (той самий документ §1.5).

**Перевірений SHA і деплой (14.09.2026, задача 10:16):** код-коміт `fc34d74`
(`fc34d740ea50fc9ac2df2d208bf058d739e11e01`), CI `Verify (TypeScript, ESLint,
tests, build)` — success, Vercel deployment `id 6434309089` (environment
Preview) — success,
`https://dreamcarvavd-jom4oowbe-6y7h9wdz4r-7375s-projects.vercel.app` (200 на
`/uk`); саме на цьому SHA перевірено рядки 10(в) і 14. `main` весь час
`ce1977af140b49dce4bb79001c7eeed5e01aa2c2` — незмінна.

**Перевірений SHA і деплой (14.09.2026, задача 12:08 — фінальний на зараз):**
код-коміт `2c8165f` (`2c8165fc611bcde39130da3515d0d4af48b31b77`), CI
`Verify (TypeScript, ESLint, tests, build)` — success, Vercel deployment
`id 6435765725` (environment Preview) — success,
`https://dreamcarvavd-4c4rx7kcs-6y7h9wdz4r-7375s-projects.vercel.app`
(200 на `/uk`); на цьому SHA перевірено рядок 17 (оновлені залежності). `main`
весь час `ce1977af140b49dce4bb79001c7eeed5e01aa2c2` — незмінна.

## Ізольована перевірка PR №28+№29 для основного сайту (14.09.2026, задача 12:08)

Задача: уточнити пакет для основного сайту (окремо від панелі) —
повторити лише те, що відрізняється від раніше звіреної комбінації
трьох PR (№27+№28+№29, сесія 11:15).

- **PR №28** (`next` 16.3.0→16.3.4): head `1c9333babb6d82aeb2dc99425230c1cf7ad840f2`, base `main` @ `ce1977af…`, mergeable.
- **PR №29** (`js-yaml` 4.3.1→4.3.2): head `9edc2be50bed1b345bb02056516b832f1420d9b3`, base `main` @ `ce1977af…`, mergeable.
- **PR №27** (`sharp` 0.35.3→0.35.4) **перекривається з №28**: №28 саме собою
  вже оновлює і `next`, і `sharp` до тих самих версій, що й №27 окремо —
  змерджені разом чи №28+№29 без №27, кінцевий стан ідентичний (перевірено
  нижче: версії збігаються з попередньою потрійною комбінацією).
- Ізольований клон `main`, `git merge` лише pr28+pr29 (без pr27, без
  локальної гілки панелі) → чистий октопус-мердж без конфліктів, сумарний
  SHA `4420cd5e5e9fdb9efbdd725ae57ccc908e674ef7`.
- Версії після мерджу: `next` 16.3.4, `sharp` 0.35.4, `js-yaml` 4.3.2 —
  **ідентичні** до потрійної комбінації №27+№28+№29 з попередньої сесії
  (очікувано, оскільки №27 і №28 цілять ту саму версію `sharp`).
- `npm ci` → **0 vulnerabilities**; `tsc --noEmit` → чисто; `next build` →
  успішно. ESLint/тести повторно не ганяв — жодна зміна між дво- і
  потрійною комбінацією не зачіпає код, який вони перевіряють (різниця —
  лише в тому, чи присутній ще один незалежний коміт з тим самим
  `sharp`-таргетом); якщо власник хоче повний прогін — займе секунди,
  скажіть.

**Рекомендація власнику:** прийняти (змерджити) №28, потім №29, у будь-якому
порядку — незалежні, конфліктів між собою не мають. №27 можна закрити як
надлишковий після прийняття №28 (не закривав сам — за межами дозволу цієї
задачі) або залишити відкритим, вирішувати власнику. Перед фактичним merge
власником стан варто звірити повторно (SHA гілок могли зміститись, якщо
Dependabot оновить PR).
