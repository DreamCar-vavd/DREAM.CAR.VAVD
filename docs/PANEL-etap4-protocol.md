# П44 Е4 — протокол цільових перевірок

**Статус (ревізовано 2026-09-10 12:50):** розділи A–D + «Preview — звичайний
цикл» нижче — це докази, отримані на коді `21d9db4`; вони **зберігаються**.
Ревізія задачі 12:50 додає: (1) машинну ознаку `outcome:"unknown"` на сервері →
нові тести поведінки (розділ E нижче, заповнюється поетапно); (2) звірку
конкретного очікуваного результату в «Перевірити результат»; (3) знімки Preview
ключових станів + цикл через кнопку UI. Кожен новий запис так само має
**середовище** і **SHA**.

Кожен запис: **середовище** + **версія коду**. Локальні керовані затримки/обриви
відтворені стабами `window.fetch` та env-прапорцем `PANEL_DEV_SLOW_MS`
(ігнорується у production). Синтетичні дані — slug-и `zzz-refresh-test` /
`zzz-e4*`, прибрані після кожного прогону. `git diff HEAD -- public/ src/content/`
після всіх прогонів — **порожній**.

---

## A. Повільне та невдале оновлення

### A1. Повільне (рендер 8 с)
**Середовище:** local `next dev` (`PANEL_DEV_SLOW_MS=8000`, :3025) · **код `21d9db4`**
Дія: «Прибрати старі копії фото» → «Підтвердити — прибрати N».

```
+7 мс      CLICK «Підтвердити»
+109 мс    кнопка: "Прибираємо копії…"  disabled=true  aria-busy=true
+312 мс    статус: «Прибрано N… Оновлюємо панель…»          (запис завершено, refresh почато)
+6344 мс   + «· Оновлення триває довше, ніж зазвичай…»       (підказка на 6 с, НЕ помилка)
+8783 мс   кнопка знову "Прибрати старі копії фото" disabled=false
           + «· Готово — панель оновлено.»                   (тільки після РЕАЛЬНОГО завершення)
```
Висновок: за 8-с оновлення кнопка **не** розблокувалася на 2.5 с. Підказка
«довше» — окремо, не завершує очікування.

### A2. Швидке (без затримки)
**Середовище:** local `next dev` (:3025) · **код `21d9db4`**
```
+607 мс   CLICK «Підтвердити»
+793 мс   кнопка "Перевіряємо…" disabled=true; статус «Прибрано N… Оновлюємо панель…»
+1236 мс  кнопка розблокована; «· Готово — панель оновлено.»
```
Висновок: швидке оновлення **не** чекає зайвого таймера (розблок 1.2 с).

### A3. Невдале оновлення (RSC-запит кидає помилку)
**Середовище:** local `next dev` (:3025), стаб кидає на `_rsc`-запиті · **код `21d9db4`**
Результат: Next сам робить **hard-reload** сторінки як fallback → кнопка **не
зависає**, панель показує актуальний стан. Запис (confirm очищення) **подіяв** —
свіжий сухий прогін після перезавантаження: «Зайвих копій фото немає».

---

## B. Оновлення без зміни версій

**Середовище:** local `next dev` (:3025) · **код `21d9db4`**
Confirm очищення видаляє `_pub`-файл, але **не** чіпає `published.json`
(`publishedAt` незмінний). Refresh усе одно завершується (~1.2 с) — завершення
залежить від відповіді сервера, не від діфу даних.

---

## C. Подвійне натискання під час запису й під час оновлення

**Середовище:** local `next dev` (:3025), стаб робить запис повільним (1.2 с) ·
**код `21d9db4`**. Лічильник — POST-запити до `/api/panel`.

```
3× швидкі кліки на «Прибрати старі копії фото»   → 1 POST
2× клік «Підтвердити — прибрати N»               → +1 POST  (2 всього: 1 dry + 1 confirm)
після confirm+refresh, 2× клік на (розблоковану) кнопку → +1 POST  (новий сухий прогін — ЧИТАННЯ)
```
Висновок: **дублюючого запису немає.** Синхронний `useRef`-guard + `disabled`
блокують повторний клік до наступного рендера й під час refresh.

---

## D. Втрачена відповідь ПІСЛЯ фактично виконаного запису

**Середовище:** local `next dev` (:3025), стаб: сервер відпрацьовує, відповідь
`confirm` відкидається · **код `21d9db4`**

```
+912 мс    CLICK «Підтвердити»
+2118 мс   дія ЗАБЛОКОВАНА; статус (role=status, aria-live=polite):
           «Відповідь від сервера не отримано — невідомо, чи застосовано дію.
            Натисніть «Перевірити результат» (лише читає стан) перед тим, як повторювати.»
           кнопка «Перевірити результат»
+10 мс     CLICK «Перевірити результат» → «Перевіряємо стан…»
+552 мс    «Стан оновлено — подивіться, чи лишилися зайві копії…»; дія РОЗБЛОКОВАНА
+1699 мс   свіжий сухий прогін → «Зайвих копій фото немає»
```
Висновок: запис **дійсно подіяв**; авто-повтору **не було**; повторити дію можна
лише після читання актуального стану.

---

## Preview — звичайний цикл очищення

**Середовище:** Vercel Preview (branch alias), авторизована сесія власника ·
**код `21d9db4`** (deployment `21d9db4bf`)
Синтетичні `_pub` `zzz-e4/_pub/*.jpg` створено через GitHub API (коміти
`34d0ae0`, `2c4720e`).

```
POST /api/panel {action:cleanup-frozen-media, confirm:false}  → count 2, headSha 2c4720e9b…
POST /api/panel {action:cleanup-frozen-media, confirm:true}   → «Прибрано 2 стар(у/і/их) копі(ю/ї/й) фото. Зміни в гілці після наступної збірки.»
POST /api/panel {action:cleanup-frozen-media, confirm:false}  → «Зайвих копій фото немає — усе прибрано.»
```
На GitHub: обидва файли → 404; **один** атомарний коміт `866b271` «panel: drop 2
unreferenced media file(s)» лише з тими 2 шляхами. Чистий діф
`21d9db4..866b271` — **порожній**.

---

## E. Цільові перевірки виправлень ревізії 12:50 (unit)

**Середовище:** `node --test` (без DOM) · **код `a87d188`+ (ревізія П44)**
Логіка кнопки винесена в чисті хелпери `routeWriteResponse` /
`routeCheckResponse` / `shouldFireAction` (`src/app/panel/actionMessages.ts`) і
`checkActionResult` (`src/lib/content/panelStore.ts`) — перевіряється **поведінка**
(блок / оновлення / дозвіл на повтор), не тексти.

| # | Сценарій задачі | Тест | Очікування |
|---|---|---|---|
| 1 | запис виконано, відповідь браузеру втрачена | `route write: scenario 1` + `check publish: true only after…` | `routeWriteResponse(null)` → **блок**, без refresh; далі `checkActionResult` після реального запису → `applied:true` → розблок |
| 2 | GitHub не підтвердив, браузер отримав JSON-помилку | `route write: scenario 2` | чистий 409 → **не блок**, повтор дозволено; `outcome:"unknown"` → **блок** |
| 3 | відмова до запису | `route write: scenario 3` + `a panel action reports a TRANSIENT failure…` | `transient:true` → **не блок**, повтор після паузи |
| 4 | перевірка результату теж з помилкою | `route check: … a failed check is not success` + `check: a read failure is NOT an answer` | `routeCheckResponse(null)` / `applied:null` → **блок лишається**, ніколи не «успіх» |
| 5 | інший редактор змінив дані між дією і перевіркою | `check publish: another editor moved the published token…` | `applied:null` → блок лишається |
| 6 | статус той самий, вміст змінився | `check publish: general status unchanged but working CONTENT changed` | `applied !== true` (рядок знову «modified») |
| 7 | швидкі повторні кліки під час запису й оновлення | `scenario 7 — no request may fire while…` + `… performs at most one write` | під час `busy`/`refreshing`/`locked` клік **не** дає запиту; ≤1 `writeFile` на дію |
| 8 | повільне оновлення / оновлення без змін даних | локально A1/A2/B вище (з вимірами) | 8с → блок 8.8с; швидкий → 1.2с; без змін → теж завершується |

Плюс `check is READ-ONLY — it never writes`: `checkActionResult` для всіх 5 дій
→ **0** `writeFile`. Повне перезавантаження після невдалого refresh (A3) —
Next сам робить hard-reload, кнопка не зависає, помилка читання не стає
«успіхом».

Разом: **400 тестів pass**, tsc 0, eslint 0, build OK, content:guard OK.

---

## F. Жива перевірка Preview через КНОПКУ в UI (ревізія Е6)

**Середовище:** Vercel Preview (branch alias), авторизована сесія власника в
Chrome — **вже працює, вхід не потрібен** · **код `ba9d236`** (CI + Vercel
success). Синтетичні `_pub`: `public/images/cms/services/zzz-e5/_pub/{aaaa…,bbbb…}.jpg`
через GitHub API — коміт `cea07bf` (не-force ref-update).

Через **кнопку** внизу `/panel` (не `POST`):

```
[клік] «Прибрати старі копії фото»
   → кнопка → «Підтвердити — прибрати 2» + «Скасувати»
   → зелене: «Можна прибрати 2 стар(і) копії фото — 1 КБ. Натисніть ще раз, щоб підтвердити.»
[клік] «Підтвердити — прибрати 2»
   → панель оновилася (router.refresh), кнопка повернулась у спокій
```

На GitHub: обидва файли → відсутні; **ОДИН** атомарний коміт `8dd6cbc`
«panel: drop 2 unreferenced media file(s)» рівно з тими 2 шляхами.
`git diff ba9d236..8dd6cbc` — **порожній**; `git diff 21d9db4..8dd6cbc --
public/ src/content/` — **порожній**; `main` @ `ce1977af` — не зрушений.

## G. Локальні знімки станів (штучні обриви — з підписом)

**Середовище:** local `next dev` (:3025), стаби `window.fetch` на `/api/panel` ·
**код `8dd6cbc`**

| Стан | Стаб | Результат в UI |
|---|---|---|
| Невизначений запис | cleanup confirm → `{ok:false, outcome:"unknown"}` (503) | кнопка **заблокована**; амбер «Відповідь від GitHub не надійшла, тому невідомо, чи збережено «копію фото». Оновіть сторінку й перевірте…»; посилання **«Перевірити результат»** |
| Перевірка → застосовано | `check-result` → `{applied:true}` | **розблок**; зелене «Схоже, копії прибрано — жодного файла з плану вже немає. · Готово — панель оновлено.»; посилання зникло |
| Перевірка → невизначено | `check-result` → `{applied:null}` | кнопка **лишається заблокованою**; амбер «Прибрано частину (1 з 2) — результат невизначений, перевірте ще раз.»; «Перевірити результат» **лишається** |
| 375 px | — | `/panel` без горизонтального скролу, кнопки/бейджі переносяться (справжній viewport, Browser pane) |

Стани «очікування» / «Оновлення триває довше…» — виміряні локально в розділах
A1/A2 вище (`PANEL_DEV_SLOW_MS`), поведінка не змінилась.

---

## Залишок (чесно)

- Повний Keystatic-цикл `zzz-test-panel` (створення→публікація→видалення) через
  UI саме на Preview — не проганявся цією ревізією: потребує тривало активної
  сесії власника. Покрито П35 (повний цикл, `docs/PANEL-test-zzz-run-20260909.md`)
  + 400 юніт-тестами. Цикл **очищення** через кнопку — виконано (розділ F).
- Справжній 375 px саме на **Preview** `/panel` — `claude-in-chrome` не емулює
  CSS-viewport для авторизованої сесії; знято локально (розмітка `/panel`
  ідентична на Preview і локально — це не публічна сторінка з жорстким CSP).
