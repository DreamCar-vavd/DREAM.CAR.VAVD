# Шаблон для GitHub App setup. Скопіювати в `.env.local` у корені проєкту,
# зробити setup (docs/PANEL-hosting-and-approvals.md §3.1), потім `.env.local`
# ВИДАЛИТИ (інакше локальні dev/build ідуть у github-режим без токенів).
# Тут немає секретів — лише режим + репозиторій.
NEXT_PUBLIC_KEYSTATIC_STORAGE_KIND=github
KEYSTATIC_GITHUB_REPO_OWNER=DreamCar-vavd
KEYSTATIC_GITHUB_REPO_NAME=DREAM.CAR.VAVD
